{
  "name": "mtmdsh",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": {
    "node": ">=22.19.0"
  },
  "scripts": {
    "build": "pnpm run build:mtm-codebase-memory && tsc -p packages/plugin-hello/tsconfig.json",
    "build:mtm-codebase-memory": "pnpm --filter mtm-codebase-memory run build",
    "demo": "node packages/plugin-hello/dist/demo.js",
    "check": "pnpm run build:mtm-codebase-memory && pnpm --filter mtm-codebase-memory run test && tsc -p packages/plugin-hello/tsconfig.json && node packages/plugin-hello/dist/demo.js && node scripts/verify-package.mjs",
    "check:mtm-codebase-memory": "pnpm run build:mtm-codebase-memory && pnpm --filter mtm-codebase-memory run test && node scripts/verify-package.mjs",
    "smoke:mtm-codebase-memory": "node scripts/verify-dsh-web.mjs",
    "verify:package": "node scripts/verify-package.mjs",
    "pack:mtm-codebase-memory": "pnpm --filter mtm-codebase-memory pack --pack-destination dist/npm"
  },
  "devDependencies": {
    "typescript": "6.0.3"
  }
}
