{
  "name": "mtmdsh",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "engines": { "node": ">=22.19.0" },
  "scripts": {
    "build": "pnpm --filter mtmharness run build",
    "check": "pnpm run check:references && pnpm --filter mtmcanvas run build && pnpm --filter mtmharness run check && pnpm --filter mtmcanvas run check",
    "check:references": "node scripts/check-workspace-references.mjs"
  },
  "devDependencies": { "typescript": "6.0.3" }
}
