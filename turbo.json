{
  "$schema": "https://turborepo.dev/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["lib/**", "dist/**"]
    },
    "typecheck": {},
    "test": {
      "dependsOn": ["build"]
    },
    "check": {
      "dependsOn": ["build"]
    }
  }
}
