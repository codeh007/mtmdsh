/** Host loader entry for the mtmcanvas browser plugin. */

export const name = "mtmcanvas";

/** The first release has no Host-side provider; the client half owns the fixture view. */
export function apply(): void {}
